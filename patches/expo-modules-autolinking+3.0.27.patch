diff --git a/node_modules/expo-modules-autolinking/build/commands/generateModulesProviderCommand.js b/node_modules/expo-modules-autolinking/build/commands/generateModulesProviderCommand.js
index 0fe4be5..34a1bbc 100644
--- a/node_modules/expo-modules-autolinking/build/commands/generateModulesProviderCommand.js
+++ b/node_modules/expo-modules-autolinking/build/commands/generateModulesProviderCommand.js
@@ -24,8 +24,12 @@ function generateModulesProviderCommand(cli) {
             appRoot: commandArguments.appRoot ?? (await autolinkingOptionsLoader.getAppRoot()),
         });
         const expoModulesResolveResults = await (0, resolveModules_1.resolveModulesAsync)(expoModulesSearchResults, autolinkingOptions);
-        const includeModules = new Set(commandArguments.packages ?? []);
-        const filteredModules = expoModulesResolveResults.filter((module) => includeModules.has(module.packageName));
+        // CocoaPods can pass an incomplete package allowlist when regenerating
+        // the provider during an SDK 54 archive. The
+        // resolver has already applied the project's search paths and excludes,
+        // so use its complete Apple module set to avoid compiling a partial
+        // ExpoModulesProvider.
+        const filteredModules = expoModulesResolveResults;
         await (0, generatePackageList_1.generateModulesProviderAsync)(filteredModules, {
             platform,
             targetPath: commandArguments.target,
diff --git a/node_modules/expo-modules-autolinking/src/commands/generateModulesProviderCommand.ts b/node_modules/expo-modules-autolinking/src/commands/generateModulesProviderCommand.ts
index cfb219d..58ab528 100644
--- a/node_modules/expo-modules-autolinking/src/commands/generateModulesProviderCommand.ts
+++ b/node_modules/expo-modules-autolinking/src/commands/generateModulesProviderCommand.ts
@@ -47,10 +47,12 @@ export function generateModulesProviderCommand(cli: commander.CommanderStatic) {
           autolinkingOptions
         );
 
-        const includeModules = new Set(commandArguments.packages ?? []);
-        const filteredModules = expoModulesResolveResults.filter((module) =>
-          includeModules.has(module.packageName)
-        );
+        // CocoaPods can pass an incomplete package allowlist when regenerating
+        // the provider during an SDK 54 archive. The
+        // resolver has already applied the project's search paths and excludes,
+        // so use its complete Apple module set to avoid compiling a partial
+        // ExpoModulesProvider.
+        const filteredModules = expoModulesResolveResults;
 
         await generateModulesProviderAsync(filteredModules, {
           platform,
